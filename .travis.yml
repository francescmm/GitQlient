language: cpp

git:
  depth: 1

sudo: required
dist: bionic

install:
    - bash ci-scripts/$TRAVIS_OS_NAME/install.sh

script:
    - bash ci-scripts/$TRAVIS_OS_NAME/build.sh

matrix:
  include:
      - os: osx
        osx_image: xcode11.2
      - os: linux
        compiler: gcc

deploy:
  provider: releases
  api_key: "783142cee384dc5def7cb12cfd048808599bae9b"
  file: "GitQlient"
  skip_cleanup: true
  draft: true
